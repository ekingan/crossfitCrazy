<div class="container">
	<div class="row">
		<div class="col-md-8 col-md-offset-3">
			<% @gyms.each do |gym| %>
			<h3>
				 <%= link_to gym.name, gym_path(gym.id) %> 
			</h3>
			<% end %>	
		</div>
	</div>
</div>

